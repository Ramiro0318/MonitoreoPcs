<UserControl x:Class="Servidor.Views.HistoricoView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Servidor.Views"
      xmlns:viewmodel="clr-namespace:Servidor.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodel:ServerViewModel}"
      mc:Ignorable="d">

    <Border Margin="20" Padding="10"  BorderThickness="3" BorderBrush="#427AA1" CornerRadius="10" Background="White">
        <StackPanel Orientation="Vertical">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="0">
                    <Image Source="/Images/computadora-portatil.png" Width="50" Height="50" Margin="0,0,10,0"></Image>
                    <TextBlock FontWeight="SemiBold" Foreground="#064789"  FontSize="20" VerticalAlignment="Center">Computadoras sin conexión reciente</TextBlock>
                </StackPanel>
            </Grid>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="105"></ColumnDefinition>
                    <ColumnDefinition Width="70" ></ColumnDefinition>
                    <ColumnDefinition Width="100"></ColumnDefinition>
                    <ColumnDefinition Width="110"></ColumnDefinition>
                    <ColumnDefinition Width="160"></ColumnDefinition>
                    <ColumnDefinition Width="85"></ColumnDefinition>
                </Grid.ColumnDefinitions>


                <Label Grid.Column="0" HorizontalAlignment="Left"  >Nombre</Label>
                <Label Grid.Column="1" HorizontalAlignment="Center">IP</Label>
                <Label Grid.Column="2" HorizontalAlignment="Center">Laboratorio</Label>
                <Label Grid.Column="3" HorizontalAlignment="Center">MAC</Label>
                <Label Grid.Column="4" HorizontalAlignment="Center">Última conexión</Label>
                <Label Grid.Column="5" HorizontalAlignment="Right" >Comandos</Label>
            </Grid>

            <ListBox Height="130" ItemsSource="{Binding Computadoras}" SelectedItem="{Binding ComputadoraSeleccionada, Mode=TwoWay}" ScrollViewer.HorizontalScrollBarVisibility="Hidden" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="105"></ColumnDefinition>
                                <ColumnDefinition Width="70" ></ColumnDefinition>
                                <ColumnDefinition Width="100"></ColumnDefinition>
                                <ColumnDefinition Width="110"></ColumnDefinition>
                                <ColumnDefinition Width="160"></ColumnDefinition>
                                <ColumnDefinition Width="85"></ColumnDefinition>
                            </Grid.ColumnDefinitions>


                            <TextBlock HorizontalAlignment="Left" Text="{Binding Nombre}" Grid.Column="0"></TextBlock>
                            <TextBlock HorizontalAlignment="Center" Text="{Binding Ip}" Grid.Column="1"></TextBlock>
                            <TextBlock HorizontalAlignment="Center" Text="{Binding Laboratorio }" Grid.Column="2"></TextBlock>
                            <TextBlock HorizontalAlignment="Center" Text="{Binding MAC }" Grid.Column="3"></TextBlock>
                            <TextBlock HorizontalAlignment="Center" Text="{Binding TiempoDesconectada }" Grid.Column="4"></TextBlock>
                            <Button Background="White" BorderBrush="White" Grid.Column="5" Width="20" Command="{Binding DataContext.EliminarCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" CommandParameter="{Binding}">
                                <Image Source="/Images/delete.png" Width="16" Height="16" ></Image>
                            </Button>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Border>
</UserControl>
