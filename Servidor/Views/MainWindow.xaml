<Window x:Class="Servidor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Servidor"
        xmlns:viewmodel="clr-namespace:Servidor.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodel:ServerViewModel}"
        mc:Ignorable="d"
        Title="Servidor" Height="850" Width="800">
    <Window.DataContext>
        <viewmodel:ServerViewModel></viewmodel:ServerViewModel>
    </Window.DataContext>
    <StackPanel Orientation="Vertical">
        <Label>Registro</Label>
        <Label Content="{Binding ComputadoraSeleccionada.Identificador}"></Label>
        <Label Content="{Binding ComputadoraSeleccionada.Ip}"></Label>
        <Label Content="{Binding ComputadoraSeleccionada.Puerto}"></Label>
        <Button Command="{Binding RegistrarCommand}" CommandParameter="{Binding ComputadoraSeleccionada}">Registrar</Button>

        <Label Content="{Binding Info}"></Label>

        <Label>Lista de computadoras:</Label>
        <ListBox ItemsSource="{Binding Computadoras}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Identificador}"></TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Label>Lista de historial de conexiones:</Label>
        <ListBox ItemsSource="{Binding HistorialComputadoras}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding HoraConexion}"></TextBlock>
                        <TextBlock Text="{Binding Identificador}"></TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </StackPanel>
</Window>
